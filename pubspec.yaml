name: map_app
description: "Travel Stop Alarm App with Time-Based Smart Monitoring"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.5.1
  geolocator: ^11.0.0
  android_alarm_manager_plus: ^4.0.3
  flutter_local_notifications: ^17.1.0
  permission_handler: ^11.3.0
  sqflite: ^2.3.3
  path: ^1.9.0
  flutter_map: ^6.1.1
  latlong2: ^0.9.1
  http: ^1.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

